{
  server {
    host = "localhost"
    port = "31516"
  }

  regional-api-key = "secret-api-key"

  email {
    sender = ""
    smtp-username = ${SMTP_USER}
    smtp-password = ${SMTP_PASSWORD}
    smtp-host = ""
    smtp-port = 1
  }

  mongo {
    host = "localhost",
    port = 27017,
    user = ${MONGO_USER},
    password = ${MONGO_PASSWORD},
    database = "campus-compass-regional"
  }

  redis {
    redis-uri = "redis://localhost"
  }

  jwt {
    sha-256-key = ${JWT_KEY}
  }
}